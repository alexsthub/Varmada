import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

export default class MyPickupScreen extends React.Component {
  render() {
    return (
      <Text style={{marginTop: 50}}>My Pickups</Text>
    );
  }
}